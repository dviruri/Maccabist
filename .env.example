# Crest importer: no API credentials are currently required.
# The Wikimedia/Wikidata public APIs are anonymous. If a keyed provider adapter
# (e.g. football-data.org) is ever enabled, its key goes here and is never committed:
# FOOTBALL_DATA_API_KEY=
