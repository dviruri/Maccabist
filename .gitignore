node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage
tsconfig.tsbuildinfo

# Local screenshot output from the dev gallery (scripts are committed, images are not).
.shots/
scripts/.crest-cache.json
